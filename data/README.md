# Data Sources

This directory contains static precomputed datasets required for SHADE.
In a production deployment, this data would be populated by spatial joins and upstream pipelines. For the hackathon, we use static fallback files.

- `svi/`: Social Vulnerability Index from CDC
- `canopy/`: Tree canopy density maps
- `osm/`: OpenStreetMap extracts (building footprints, roads)
